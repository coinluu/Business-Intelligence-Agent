# Contributing

Keep changes scoped, add regression tests for behavior changes, and preserve truthful failure reporting. Pull requests must pass unit tests, profile compilation, package build, secret-pattern scanning, and platform scheduling tests where applicable.

Never commit `.env.local`, `user-profile.yaml`, output databases, runtime files, scheduler state, logs, or generated Obsidian reports.
